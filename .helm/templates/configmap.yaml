apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "local.labels" . | nindent 4 }}
data:
  app-config.json: |-
{{ .Files.Get "files/app-config.json" | indent 4 }}
